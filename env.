# --------------------------------------
# پورت سرور
PORT=3000

# دامنه ای که اجازه درخواست به سرور دارد
ALLOWED_ORIGIN=http://localhost:3000

# --------------------------------------
# تنظیمات SMTP (برای ارسال ایمیل)
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=your_smtp_username
SMTP_PASS=your_smtp_password

# ایمیل فرستنده (در header ایمیل ظاهر می‌شود)
FROM_EMAIL=no-reply@yourdomain.com

# ایمیل صاحب سایت (سفارش‌ها به این ایمیل می‌روند)
OWNER_EMAIL=planvestfirst@gmail.com

# --------------------------------------
# آدرس قرارداد (بعدا می‌دی)
CONTRACT_ADDRESS=0x0000000000000000000000000000000000000000

# توکن ساده برای مشاهده سفارش‌ها در مسیر admin
ADMIN_TOKEN=your-secret-admin-token
